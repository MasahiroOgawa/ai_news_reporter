# ============================================
# AI News Reporter - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# ----- LLM API Keys -----
# Claude (Anthropic) - used when llm.provider is "claude" in config.yaml
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# OpenAI - used when llm.provider is "openai" in config.yaml
OPENAI_API_KEY=sk-xxxxx

# ----- Search API -----
# Tavily API for news search
TAVILY_API_KEY=tvly-xxxxx

# ----- Email (SMTP) -----
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# ----- Slack Bot -----
# Bot OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-xxxxx

# ----- Optional Settings -----
# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Config file path (default: config.yaml)
CONFIG_PATH=config.yaml
